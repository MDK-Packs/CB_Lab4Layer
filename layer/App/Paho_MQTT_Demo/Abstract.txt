Paho MQTT Demo

This demo application connects to Eclipse test server through MQTT,
subscribes to topics and publishes messages.

1. Configure WiFi connection settings in socket_startup.c (when WiFi is used):
 - SSID:          network identifier
 - PASSWORD:      network password
 - SECURITY_TYPE: network security

2. Build the application

3. Connect the debugger

4. Run the application and observe the messages in the Debug (printf) window
   or external Terminal
